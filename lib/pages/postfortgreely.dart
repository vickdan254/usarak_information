import 'package:flutter/material.dart';

class PostFortGreely extends StatelessWidget {
  // final String pageText;

  // postFortGreely(this.pageText);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Greely'),
      ),
      body: Center(
        child: Text('Greely'),
      ),
    );
  }
}
